# Principles — 온톨로지 강화

1. **손실불가**: 기존 169 tests + NDCG + 3,637 노드 무손실
2. **점진적 해제**: Gate를 한번에 다 열지 않고, 하나씩 열며 측정
3. **데이터 기반 튜닝**: threshold는 가설이 아닌 실측으로 결정
4. **LLM 비용 0 우선**: config 변경, 로직 수정으로 해결 가능한 것 먼저
5. **후방 호환**: 기존 recall_log, edges, nodes 데이터 보존
